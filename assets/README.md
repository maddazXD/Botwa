# Assets

Taruh gambar-gambar base buat command sticker template di sini.

- `ustadz.jpg` — dipakai oleh command `/ustadz <teks>`. Belum ada default-nya
  (sengaja, biar gambarnya kamu yang pilih sendiri) — upload file JPG/PNG
  ke sini dengan nama persis `ustadz.jpg` biar command-nya jalan.
